# `@embedearth/map`

The map client currently ships from `@embedearth/sdk/map` so the public foundation can stabilize one install path. This package boundary is reserved for a future split when the map API has independent release needs.
