# Features

The built-in registry contains the stable source IDs 101 (restaurant), 107
(hospital), and 113 (school), with plural aliases. Numeric IDs are permanent
once published. Core accepts numeric IDs; the SDK resolves keys and aliases.
