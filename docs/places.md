# Places

Places are exposed through the feature registry and search interface. Discover available feature keys with the SDK or CLI, then query them by area, bounding box, or exact region identifier.

```bash
earth features list
earth features search hospital
earth search hospital --area Toronto
```
