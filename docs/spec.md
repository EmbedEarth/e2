# E2 specification

An E2 cell combines an H3 spatial cell with a fixed UTC temporal bucket and an
optional permanent numeric feature ID. Canonical forms are
`e2:1:<h3>:<resolution>:<bucket>` and
`e2:1:<h3>:<resolution>:<bucket>:<feature-id>`. `BUILD.md` is the normative
repository implementation contract.
