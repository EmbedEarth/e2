# Nearby search

Use spatial compute to find one feature near another, then export the result as JSON for an application.

Try `earth compute nearest hospital --area Manhattan --near subway --distance 1000 --json`.
